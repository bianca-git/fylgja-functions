rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if true; // TODO: Replace with proper auth
    }
    match /tasks/{taskId} {
      allow read, write: if true; // TODO: Replace with proper auth
    }
    match /checkins/{checkinId} {
      allow read, write: if true; // TODO: Replace with proper auth
    }
  }
}
